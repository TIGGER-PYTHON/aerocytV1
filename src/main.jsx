import React from 'react'
import ReactDOM from 'react-dom/client'
import LungScreeningApp from './LungScreeningApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LungScreeningApp />
  </React.StrictMode>,
)
